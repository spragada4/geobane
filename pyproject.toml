[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "geobane"
version = "0.1.0"
description = "Adversarial test-data generator for geospatial systems"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
dependencies = []
